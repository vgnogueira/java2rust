#![allow(non_snake_case)]


#[derive(Debug)]
pub struct Employee {
    name: String,
    id: String,
    salary: f64,
}
pub fn newEmployee (name: String, id: String, salary: f64) -> Employee {
    Employee {
        this.name = name;
        ;
        this.id = id;
        ;
        this.salary = salary;
        ;
    }
}

impl Employee {

    pub fn getName(&mut self) -> String {
        return name;
    }

    pub fn getId(&mut self) -> String {
        return id;
    }

    pub fn getSalary(&mut self) -> f64 {
        return salary;
    }
}
